---
description: run DeepLab's traced research pipeline from a direction or specific topic
---

Load the bundled `ai4s-agent` skill and follow its DeepLab / dsh orchestration
contract exactly.

Starting point: $ARGUMENTS

When the starting point is empty, ask for a broad direction or specific topic,
constraints, and whether real measured results already exist. Use dedicated dsh
subagents for the pipeline stages, inspect every required workspace artifact
before continuing, and never invent citations or measured results.
